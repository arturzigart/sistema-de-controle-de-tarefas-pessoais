<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* SIDEBAR LATERAL */
        .sidebar {
            width: 220px;
            background-color: #002244; /* azul escuro */
            min-height: 100vh;         /* ocupa toda a altura da tela */
            position: fixed;           /* fixa na lateral */
            top: 0;
            left: 0;
            padding-top: 80px;         /* espaço para não cobrir a navbar */
        }

        .sidebar a {
            color: white;
            padding: 12px 18px;
            display: block;
            text-decoration: none;
            font-size: 1.1rem;
        }

        .sidebar a:hover {
            background-color: #003366;
        }

        /* CONTEÚDO PRINCIPAL */
        .content {
            margin-left: 220px; /* deixa espaço para sidebar */
            padding: 20px;
        }

        body {
            background-color: #e9f4ff;
        }
    </style>

</head>
<body>


    <!-- NAVBAR SUPERIOR (SEU CÓDIGO ORIGINAL + BOOTSTRAP) -->
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top bg-dark">
        <div class="container">
          <a class="navbar-brand fw-bold" href="#"> Sistema de Controle de Tarefas Pessoais</a>

          <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#menu">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="offcanvas offcanvas-end text-bg-dark" id="menu">
            <div class="offcanvas-header">
              <h5 class="offcanvas-title"><%= usuario.nome %></h5>
              <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
            </div>

            <div class="offcanvas-body">
              <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link active" href="#"><%= usuario.nome %></a></li>
                <li class="nav-item"><a class="nav-link" href="#">Sair</a></li>
              </ul>
            </div>
          </div>
        </div>
    </nav>

    <!-- SIDEBAR LATERAL -->
    <div class="sidebar">
        <a href="#">Cadastrar</a>
        <a href="#">Listar</a>
    </div>

    <!-- CONTEÚDO PRINCIPAL -->
    <div class="content">
        <h3>Bem-vindo ao sistema</h3>
        <p>Escolha uma opção no menu lateral.</p>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
